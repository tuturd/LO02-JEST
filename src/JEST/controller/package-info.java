/**
 * This package contains all the classes linked to the game controllers and user interactions :
 * 
 * <ul>
 *   <li>{@link GameController} is the interface that defines all interactions between the game and the user.</li>
 *   <li>{@link GUIController} is the graphical implementation that handles all interactions through Swing components.</li>
 *   <li>{@link PlayerRouter} is a router that delegates player-specific operations to the appropriate controller based on each player's preferred interface (GUI or Console).</li>
 * </ul>
 */
package JEST.controller;
