/**
 * This package contains the controller of the application :
 * {@link ConsoleController} : It permits the interactions between the user and the console, using the scanner.
 * {@link GameController} : Interface that defines all interactions between the game and the user.
 * {@link GUIController} : Graphical User Interface implementation using existing View classes.
 * {@link PlayerRouter} : It delegates player-specific operations to the appropriate controller based on the player's preferred interface (GUI or Console).
 */
package JEST.controller;